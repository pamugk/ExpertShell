package base.environmentvars;

//Tracing type
public enum TRAC {
    N, //trace nothing
    F, //trace fulfilled rules
    C, //trace considered and fulfilled rules
    V //trace considered and fulfilled rules, with unknown vars (verbose)
}
